import { MoonDirective } from './moon.directive';

describe('MoonDirective', () => {
  it('should create an instance', () => {
    const directive = new MoonDirective();
    expect(directive).toBeTruthy();
  });
});
